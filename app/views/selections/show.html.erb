<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @selection.name %>
</p>

<%= link_to 'Edit', edit_content_pack_selection_path(@selection) %> |
<%= link_to 'Back', selections_path %>
